var assert = require('assert')
, hubot-zoi = require('../lib/hubot-zoi');

describe('Setting up Mocha', function(){
  it('should run a simple test', function(done){
    assert.strictEqual(true, true);
    done();
  })
})  
